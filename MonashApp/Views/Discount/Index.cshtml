@model IEnumerable<MonashApp.Models.Discount>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Staff)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tier1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tier2)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tier3)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Staff)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tier1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tier2)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tier3)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
